# Dependencies and build output
node_modules/
.expo/
.expo-shared/
dist/
web-build/
coverage/

# Native project dirs (generated by prebuild / managed by EAS)
android/
ios/

# Secrets and local-only config: build-time env comes from eas.json profiles.
# .env holds this machine's LAN/VPS host values and must stay local.
.env
.env.*
!.env.example

*.log
*.bak
eas.json.bak

# OS cruft
.DS_Store
Thumbs.db
